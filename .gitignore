# toolchain + checkout (fetched by scripts/, never committed)
/chromium/
/.cache/
/.dev-profile/

# local-only monitoring dashboard (personal, stays off git)
/monitor/

# scratch build logs (local, reproducible from scripts/build.sh)
/.buildlogs/

.DS_Store
node_modules
